#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("file.in","r",stdin);
    freopen("file.out","w",stdout);
    int n = 0 , a[1000];
    for(int i = 0 ; i < n ; i ++)
        cin >> a[i];
    for(int i = 0 ; i < n ; i ++)
        cout << a[i];
    fclose(stdin);
    fclose(stdout);
    return 0;
}